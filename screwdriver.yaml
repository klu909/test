shared:
  image: node:18
jobs:
  main:
      requires: [ ~commit ]
      steps:
        - sleep: meta set build.warning true
        - fail: return 1
  green:
      requires: [main]
      steps:
        - hello: echo hello
