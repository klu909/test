shared:
  image: node:18
  settings:
    slack:
      channels:
        - test-bot-notification
      statuses:
        - SUCCESS
        - FAILURE
        - QUEUED
        - RUNNING
      minimized: false
jobs:
  main:
    requires: [ ~pr, ~commit ]
    steps:
      - echo hi
