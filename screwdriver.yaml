shared:
  image: node:18
jobs:
  main:
      requires: [ ~pr, ~commit ]
      steps:
        - warn: meta set build.warning.warn "this is a warning"
        - alert: meta set build.warning.alert "this is an alert"
  main2:
      steps:
        - warn: meta set meta.summary.warn "this is a warning"
        - alert: meta set meta.summary.alert "this is an alert"
